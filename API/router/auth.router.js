const { Router } = require('express')

const router = Router()

// http://localhost:3000/auth/login
// Autentica el usuario con la base de datos
// TODO: ¿Cómo implementar cookies de autenticación al iniciar sesión?
router.post('/login', async (req, res) => {
    try {
        
    } catch (error) {
        
    }
})

module.exports = router